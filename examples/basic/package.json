{
  "name": "sonik-example-basic",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "dev:pages": "wrangler pages dev --compatibility-date=2023-07-01 --live-reload ./dist",
    "deploy": "wrangler pages deploy ./dist"
  },
  "dependencies": {
    "hono": "^3.3.4",
    "sonik": "^0.0.41"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "vite": "^4.4.6",
    "wrangler": "^3.4.0"
  }
}